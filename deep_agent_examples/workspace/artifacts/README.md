# 本地 Shell 运行产物

这个目录保存 `local_shell_agent` 在演示过程中生成的文件，例如 `hello.py`。

它不是独立示例，不需要单独启动。请从项目根目录运行：

```powershell
uv run deep-agent-example local-shell --approve --thread-id local-001
```

除本 README 和 `.gitkeep` 外，本目录中的运行产物默认被 Git 忽略。
